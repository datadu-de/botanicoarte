---
import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import type { Locale } from '../i18n/translations';

interface Props {
  title: string;
  description?: string;
  locale: Locale;
  currentPath?: string;
}

const { title, description, locale, currentPath = '/' } = Astro.props;
---

<!doctype html>
<html lang={locale}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description ?? 'Arte Botanico – Art Studio & Atelier, Mallorca'} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;1,300;1,400;1,500&family=Inter:wght@300;400;500&display=swap"
      rel="stylesheet"
    />
    <link rel="alternate" hreflang="en" href="/en{currentPath === '/' ? '' : currentPath}" />
    <link rel="alternate" hreflang="de" href="/de{currentPath === '/' ? '' : currentPath}" />
    <link rel="alternate" hreflang="x-default" href="/en{currentPath === '/' ? '' : currentPath}" />
    <title>{title}</title>
  </head>
  <body>
    <Header locale={locale} currentPath={currentPath} />
    <main>
      <slot />
    </main>
    <Footer locale={locale} />
  </body>
</html>
